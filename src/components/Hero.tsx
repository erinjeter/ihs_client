import React, { Component } from 'react';

class Hero extends Component {
    constructor(props: any){
        super(props);
    }

    render(){
        return (
            <div className="container bg-hero-image bg-center max-w-full bg-top h-screen">
                
            </div>
        )
    }
}

export default Hero;